schemaVersion: "1.2"
endpoints:
  - name: "rest-api-be2"
    displayName: "REST API"
    service:
      basePath: /
      port: 8000
    type: REST
    networkVisibility:
      - Project
      - Public

env:
  - name: DATABASE_URL
    value: ${env:DATABASE_URL}
